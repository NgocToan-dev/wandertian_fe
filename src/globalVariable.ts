import Command from "./utilities/enum/Command";
import EditMode from "./utilities/enum/EditMode";
import PostStatus from "./utilities/enum/PostStatus";
import ValidateSave from "./utilities/enum/ValidateSave";

export const globalVariable = {
  Command,
  EditMode,
  PostStatus,
  ValidateSave
};
